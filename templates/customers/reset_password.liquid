<div class="section">
  <div class="page-width-narrow">
    <h1 style="font-size:3rem">Reset password</h1>
    {% form 'reset_customer_password' %}
      <div style="display:grid;gap:1rem;margin-top:1.5rem">
        <input class="field" type="password" name="customer[password]" placeholder="New password">
        <input class="field" type="password" name="customer[password_confirmation]" placeholder="Confirm password">
        <button class="btn" type="submit">Reset</button>
      </div>
    {% endform %}
  </div>
</div>
